# Test Project — Sample Dashboard

This is a minimal React + TypeScript + Vite project demonstrating a simple dashboard UI with simulated data.

Quick start

```bash
npm install
npm run dev
```

Open http://localhost:3000

Notes
- Uses MUI for UI components.
- Routes are configured in `App.tsx`.
# test-project
